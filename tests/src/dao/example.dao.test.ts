//-----------------------------------------------------------------------------
// tests/src/dao/example.dao.test.ts
//-----------------------------------------------------------------------------

describe(`ExampleDAO`, () => {
  describe(`Jest verification test`, () => {
    it(`Runs the jest test`, () => {
      const sum = 5 + 4
      expect(sum).toBe(9)
    })
  })
})